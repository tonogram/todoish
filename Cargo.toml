[package]
name = "todoish"
version = "0.0.1"
edition = "2021"

[dependencies]
eframe = "0.17"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
dirs = "4.0"

[profile.release]
lto = true
strip = true